# === API ===
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9       # upload file
pydantic>=2.0.0
pydantic-settings>=2.0.0

# === HTTP client (gọi AI service) ===
httpx>=0.27.0

# === Storage ===
pymongo>=4.6.0                # lịch sử job BE + đọc lịch sử đề của AI
minio>=7.2.0                 # đọc ảnh crop từ MinIO để convert base64

# === Utilities ===
loguru>=0.7.0
python-dotenv>=1.0.0

# === Dev (optional) ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
